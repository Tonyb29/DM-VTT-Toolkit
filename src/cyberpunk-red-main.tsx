import React from 'react'
import ReactDOM from 'react-dom/client'
import CyberpunkRedApp from './cyberpunk-red-app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CyberpunkRedApp />
  </React.StrictMode>
)

node_modules/
dist/
.DS_Store
*.local

# Separate projects — tracked in their own repos
audio-transcriber/

# Test case files — kept local only, not pushed to online repo
# Folder structure (.gitkeep) and READMEs are still committed; payloads are excluded
test-cases/**/*.json
test-cases/**/*.xml
test-cases/**/*.js
test-cases/**/*.txt

# Campaign source documents — kept local only
campaign/**/*.txt
campaign/**/*.md
campaign/**/*.pdf
campaign/**/*.docx
campaign/**/*.json
